<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Material (dark), not Material.Light: the Compose theme is unconditionally dark, so a
         light platform theme meant a white windowBackground flashing on cold start. -->
    <style name="Theme.EPSSGTracker" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/app_background</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.EPSSGTracker</item>
        <!-- Native-only on API 31+; the compat library backport (API 26-30) doesn't support a
             branding image, but silently ignores this attribute there. -->
        <item name="android:windowSplashScreenBrandingImage" tools:targetApi="31">@drawable/splash_branding</item>
    </style>
</resources>