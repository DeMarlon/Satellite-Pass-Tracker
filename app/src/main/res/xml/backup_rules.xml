<?xml version="1.0" encoding="utf-8"?>
<!--
  API 26-30 equivalent of data_extraction_rules.xml (that schema is API 31+ only). Same policy:
  excludes only, so all three settings DataStores are backed up. Keep the two files in sync.
-->
<full-backup-content>
    <exclude domain="file" path="datastore/pass_reminders.preferences_pb" />
    <exclude domain="database" path="tracker_database" />
</full-backup-content>
